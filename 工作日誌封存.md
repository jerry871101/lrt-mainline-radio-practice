# 工作日誌封存｜淡海輕軌正線無線電練習

> 已完成並實測的情境詳細紀錄移到這裡，保持 `CONTEXT.md` 精簡。
> `CONTEXT.md` 只留「目前進行中 + 待辦」。

---

## 2026-06-12（第一次工作）｜各情境修正

| 情境 | 修正內容 |
|------|---------|
| 煞車系統故障 | 步驟順序對調：先 CMS「隔離轉向架」→ 再應急「煞車解除」 |
| 車門系統故障 | CMS 改「全開全關」；應急改「上下車門」（原「零速度」錯誤） |
| 輔助速限（ATP） | 步驟1 CMS TCU復位 → 應急「ATP重置」；補 Key Off/Key On |
| 牽引系統故障 | 補「確認ATP無異常」、「高架段已切換機廠模式」 |
| 供電系統故障 | 應急改「集電弓降弓 HV OFF」；補「手動升降弓一次」 |
| 清車重開機 | 應急改「降級模式」（原「降級模式+機廠模式」錯誤） |
| 空調系統故障 | 修正「通話完畢」位置 |

## 2026-06-13（第二次工作）｜牽引態樣 A 確認

- 逐情境逐步驟人工檢核；牽引細分 3 種態樣；路段定義 V01–V07 高架、其餘平面。
- 建立全域 `AI_WORKING_RULES.md`。
- **牽引 態樣A（T字燈少一顆）完整流程已確認**（後演進為 §1.1 `fault_traction_tcu`）。

## 2026-06-15（第三次工作）｜GitHub 備份 + 位置邏輯

- GitHub 備份環境、VS Code GitDoc 自動備份。
- 新增全域規則 7（討論結束前工作總結）。
- 新增 `getAdjacentFaultLocation()`：故障位置「V07 往 V08 之間途中」格式。

## 2026-06-26（第四次工作）｜煞車互動 Bug 修復 + RWD

- **Bug：煞車情境卡死** — `handleCMSClick` 隔離轉向架分支提早 return 跳過 `completedActions.add`。改為「必須隔離正確轉向架（`persistentFault.bogie`）才解鎖」。
- **Bug：隔離後 HB 被 render 蓋回 E1** — 新增 `persistentFault.isolated` 旗標；已隔離→gear 維持 HB + ERR 標籤續顯。Playwright 實測 PASS。
- **CMS HMI 排版 RWD** — `.cms-area` 高 335px、`body padding-top` 345px；`max-width:640px` 置中；新增 `fitCMS()` 手機等比縮放。Playwright @1280/768/390 PASS。

## 2026-06-27（第五次工作）｜牽引三流程定稿 + UI 大調整

### CMS 車門按鈕改版
- `控制 > 車門` 改為「車門全開」「車門全關」兩個一般按鈕，移除佔整列的「全開全關」；`btn-door-open`/`btn-door-close`。

### CMS 頂部版面
- 總故障燈 `#master-fault-lamp`：CMS 框**右上角**（橫式），白/黃/紅，bbox 實測不擋列車。
- 後視鏡重置移到 **HB 正下方**；獨立 `#cms-t` 隱藏（保留元素避免 null）；列車左移 `margin-right:16px`。
- 380V 閃電 `#cms-380v`（弓下方）：白 `v380-normal`＝正常、黃 `v380-fault`＝動力問題。

### 藍海線站序修正
- `BLUE_SEQUENCE` 改正為 `["V01"…"V09","V28","V27","V26"]`（原順序顛倒且缺共線段）。

### 牽引三流程（下拉拆三選項，用故障排除流程命名）
- **§1.1 `fault_traction_tcu`**（T字燈消失/TCU離線）：沿用既有 generic flow。
- **§1.5 `fault_traction_redlight`**（無牽引/總故障燈紅）：`buildRedlightChunks()`。依 `_atStation` 分站內(車門全開全關兩步)/站間(降級模式)；OCC 下達清車重開機(無按鈕)；步驟4恢復(燈轉白)；收車流程 `buildPickupChunks()`（綠 V10→V11 2月台→2-0交接／藍 V27→V26 2月台→清車換端→V26 1月台備用車）。
- **§3.4 `fault_traction_380v`**（380V跳脫）：`buildV380Chunks()`。步驟1報「380V黃燈、M1 380/24V反灰」；隨機兩結局 `_v380Recovered`：A=TCU有效→「請繼續行駛持續留意觀察」；B=降級(高架切機廠)→下游站復歸→復歸無效→再降級+端點站清車重開機→恢復→收車（B 因已在端點站清車重開機，不走 V10/V11，OCC 直接下達綠→2-0重交接／藍→V26備用車，共 6 段）。

### 車門全開全關兩步
- `doorSeqStep` 狀態；`handleCMSClick` 先全開後全關才完成；render 依序高亮。`fault_door` 也改用兩步。

### Playwright 實測（全部 0 error）
- §1.5 站內/站間×綠/藍 8 段，燈 W→R→W；§3.4 結局A 3段、結局B 綠/藍各 7段，380V 黃↔白；車門兩步解鎖；版面右上不擋列車、後視鏡在 HB 下、380V 可見、獨立 T 隱藏。

### 🎉 牽引三流程（§1.1 / §1.5 / §3.4）全部定稿 + 實測完成。

## 2026-07-08（校對 #1）｜牽引-T字亮未紅燈(Bypass) 定稿＋實測

依權威文件（牽引系統 §「T字燈號有亮起、總故障燈未亮紅燈、主控制器無法出力」兩步：01 行控授權按 Bypass／02 無效→復歸應急按鈕後降弓重開機）逐句定稿。

**定稿台詞**（例：101車；`[並切換機廠模式]`＝高架段 V01–V07 故障才帶入，平面不帶）：

| 步驟 | 角色 | 台詞 |
|------|------|------|
| 1 | 司機員 | `[站間]，101車呼叫OCC，CMS首頁T字燈號有亮起、總故障燈未亮紅燈，主控制器無法出力，列車無牽引動力、無法移動，OVER` |
| 1 | OCC | `OCC收到，101車，請按壓應急按鈕「列車自動保護 Bypass」[並切換機廠模式]，確認列車是否恢復，OVER` |
| 1 複誦 | 司機員 | `101車收到，按壓應急按鈕「列車自動保護 Bypass」[並切換機廠模式]，通話完畢。` |
| 2 | 司機員 | `101車回報，已按壓應急按鈕「列車自動保護 Bypass」[並切換機廠模式]仍無效，列車無法移動，OVER` |
| 2 | OCC | `OCC收到，101車，請播放預錄廣播告知旅客相關資訊後，復歸應急按鈕並執行降弓重開機，OVER` |
| 2 複誦 | 司機員 | `101車收到，播放預錄廣播後復歸應急按鈕並執行降弓重開機，通話完畢。` |
| 3 | 司機員 | `101車回報，已完成降弓重開機，列車恢復正常、可移動，OVER` |
| 3 | OCC | `OCC收到，101車重開機後不載客，續行收車，OVER` |

之後接通用收車（已就地重開機型：下行先至 V01 正常換端無台詞；綠＝V10 1月台清除路徑→V11 2月台清車→2-0 交接／藍＝V26 1月台清車換端→攜裝備至 V26 2月台藍海備用車）。

**程式改動**：
- `FAULT_DATA.fault_traction_bypass` 改兩步定稿（recoverReport 更新）。
- 通用 renderer（`_isFaultScenario`）擴充選用欄位：`type:'report'`（回報型開頭「{車號}車回報」）、`occ`/`echo`（自訂 OCC/複誦句）、`mcMode`＋`{MC}` 記號（動態機廠模式）；無新欄位的情境輸出不變。
- 新增 `isElevatedLoc(loc)`（V 編號全 ≤7 且無 J 路口＝高架）；注入點 pt 加 `_elevated`。

**實測（jsdom 白箱）**：台詞矩陣 4 路線×40 次逐句比對全過（高架 76／平面 84 皆覆蓋）；31 情境×10 次回歸 310 runs 零 undefined/{MC}/空台詞/未終結；`fault_traction_tcu` 舊式結尾確認未動；0 console error。

---

## 校對 #2：牽引-CMS T字燈號消失(TCU離線) `fault_traction_tcu`（2026-07-10 定稿＋實測）

**三裁決點（2026-07-10 使用者拍板）：**
1. **刪 Bypass 步驟**（權威文件僅「TCU錯誤復位→若牽引圖示仍離線，於端點站重新開機」；Bypass 屬「T字亮未紅燈」獨立情境）。
2. **可移動性採方向 A（照通則）**：初報「列車無法移動」；復位後「列車恢復可移動，惟牽引圖示仍離線」。使用者補充：TCU 離線實務上車可移動、行控會指示續駛，但練習場景照通則報。
3. **隨機熄 1～2 顆 T 燈、三種情形都做**：動力轉向架僅 **M1、M5**（各 2 顆 T 燈，CMS 元件 `t-m1-1/2`、`t-m5-1/2` 共 4 顆）。

**定稿台詞（綠線下行示例；`[T燈描述]`＝隨機三選一）：**

| 步驟 | 角色 | 台詞 |
|------|------|------|
| 1 | 司機員 | `[站間]，101車呼叫OCC，CMS首頁[T燈描述]，已確認TCMS未亮紅燈，列車無法移動，OVER` |
| 1 | OCC | `OCC收到，101車，請執行CMS「TCU錯誤復位」作業，確認牽引是否恢復，OVER` |
| 1 複誦 | 司機員 | `101車收到，執行CMS「TCU錯誤復位」作業，通話完畢。` |
| 2 | 司機員 | `101車回報，已執行TCU錯誤復位，列車恢復可移動，惟牽引圖示仍離線，OVER` |
| 2 | OCC | `OCC收到，請101車繼續行駛，抵達V02 2月台回報，OVER` |
| 2 複誦 | 司機員 | `101車收到，繼續行駛抵達V02 2月台回報，通話完畢。` |

T燈描述三變體：`M1轉向架少一顆T燈`（單顆，M1/M5 隨機）／`M1轉向架兩顆T燈消失`（同架兩顆）／`M1、M5轉向架各少一顆T燈`（跨架）。
步驟 3 起＝煞車 ERR 同款「**未就地重開機型**」收車：下行 V02 2月台回報→V01 清車不載客重開機→發車→收車尾段；上行不重開機直接收車尾段。收車台詞完全沿用 `buildPickupChunks`。

**程式改動：**
- `FAULT_DATA.fault_traction_tcu` 移除（改走專屬 builder，留註解）。
- `buildPoints` 新增 tcu 注入分支：三變體隨機、`persistentFault.subtype='tcu'`＋新欄位 `tLights`、站間 loc、`_isTcuScenario`/`_tDesc`/`_isBlue`/`_uplink`。
- 新增 `buildTcuChunks()`：下行 4 chunk＋通用收車；上行 2 chunk＋通用收車。V01 重開機前 faultKey 維持 `fault_traction_tcu`（T 燈持續熄滅），重開機 chunk 起 `recovered`。
- `buildPickupChunks(t, isBlue, fkOverride)` 加選用第三參數：上行未重開機 → 收車段 CMS 維持故障顯示（既有 6 個呼叫點不受影響）。
- `updateCMSDisplay` tcu 分支改為：熄 `persistentFault.tLights`、主 T 燈 fault、診斷欄警示；**移除舊版誤植的檔位 E1**（TCMS 未紅燈→總故障燈維持白、檔位 HB 不變）。
- `handleCMSClick` TCU錯誤復位：`subtype==='tcu'` 時不觸發 300ms T 燈歸位（復位後仍離線）。
- `makeChunks` 加 `_isTcuScenario` dispatch。

**實測（jsdom 白箱，2026-07-10）**：4 路線（綠上/綠下/藍上/藍下）×30 次台詞矩陣（T燈描述↔tLights 一致、TCMS/無法移動句、收車分支、faultKey 時序、isFinal、無殘留終點）＋CMS 視覺互動（熄燈正確、總故障燈白、檔位 HB、復位鈕綠、復位後 T 燈維持熄滅）＋32 情境×4 路線×4 次回歸掃描 512 runs——**1634 項檢查全過、0 console error**。三變體出現次數 one/same/cross＝47/41/32。

---

## 校對 #3：煞車圖示 E1(無Err/Se)／EE ＋ 高架機廠模式全域規則（2026-07-10~11 定稿＋實測）

**全域寫死規則（2026-07-10 使用者訂）**：「降級模式」「列車自動保護 Bypass」「上下車門」三顆應急按鈕，高架段（V01–V07）按壓一律加「並切換機廠模式」，適用每一個情境。程式＝通用 renderer `MC_BUTTONS` 清單自動判斷（`_elevated`），occ/echo 自訂句支援 `{MC}`；已寫入《報話通用規則》§1.3。eb12/eb3/door/speed/atp_readerr 等草稿自動生效。§1.5 站間分支（定稿情境）於 2026-07-11 經使用者同意補上（buildRedlightChunks 分支乙依 `isElevatedLoc(loc)` 動態帶入 OCC＋複誦句）。

**E1(無Err/Se)（2026-07-10 定稿）**：BCU 分支不做（1組紅＝同ERR隔離、2組紅＝同兩組以上，教學重複）。流程：
1. 初報「CMS首頁出現E1煞車圖示、未伴隨Err或Se，已將主控制器回停機檔位放開DSD、推至中立位再回停機檔位重複四至五次，仍無牽引動力，列車無法移動」→ OCC「請查看TCMS內BCU是否亮紅燈」→ 複誦。
2. 回報「TCMS內BCU未亮紅燈」→ OCC「請按壓應急按鈕「降級模式」{MC}，嘗試移動列車」→ 複誦（鎖降級模式鈕）。
3. 回報「已按壓…{MC}仍無效，列車無法移動」→ OCC「播放預錄廣播…復歸應急按鈕並執行降弓重開機」→ 複誦。
4. 恢復「已完成降弓重開機，列車恢復正常、可移動」→ 通用收車（已就地重開機型）。

**EE（2026-07-11 定稿）**：結構同 E1 減 BCU 步驟；初報「CMS首頁出現EE煞車圖示，總故障燈亮紅燈、列車無法啟動，已將主控制器壓著DSD推到底3至5秒再拉回停機檔位並重複四至五次，仍無牽引動力，列車無法移動」→ 降級模式{MC}（鎖鈕）→ 無效 → 播廣播＋復歸＋降弓重開機 → 通用收車。「若能移動→下游站復歸」分支不做（與 §3.4 重複）。

**renderer 擴充**：occ/echo 自訂句支援 `{MC}` 置換；`MC_BUTTONS` 自動加 MC（`s.mcMode` 保留相容）。

**實測（jsdom）**：E1 4路線×15＋EE 4路線×15（高架/平面皆中、逐句比對）＋五情境 MC/位置一致性＋§1.5 站間 60 runs＋512 runs 回歸——累計 3061 項檢查全過、0 console error。

---

## 校對 #4：列車操作 EB1/2（倒滑）／EB3（2026-07-11 定稿＋實測）

**EB1/2（倒滑）**：結構同 EE 兩步。初報「列車倒滑導致EB1/2作動，已將主控制器移至停機檔位、放開DSD再回煞車檔位並重複四至五次，仍無牽引動力，列車無法移動」→ 降級模式{MC}（鎖鈕）→ 無效 → 播廣播＋復歸＋降弓重開機 → 通用收車。客室把手／緊急開門版屬客室動作＝C類擱置。

**EB3**：三步。初報「**稍早列車誤拉至EB3**（使用者原話），已確認集電弓、供電、電壓及空調狀態正常，並將主控制器移至停機檔位放開DSD再回煞車檔位，仍無牽引動力，列車無法移動」→ CMS「TCU錯誤復位」（鎖鈕）→ 回報仍無牽引 → 降級模式{MC}（鎖鈕）→ 無效 → 播廣播＋復歸＋降弓重開機 → 通用收車。

兩者「若能移動→下游站復歸」分支均不做（與 §3.4 重複）。實測：各 4 路線×15 逐句（高架/平面皆中）＋512 runs 回歸，累計 4334 項全過、0 console error。

---

## 校對 #5：空調 一組／二組以上壓縮機未做動（2026-07-11 定稿＋實測）

**術語（使用者定）**：不說「空調未啟動」，說「**空調壓縮機未做動**」並回報「**風扇有做動**」。初報不帶「列車無法移動」（不影響行駛，通則例外）。下拉名稱同步改。

**一組（fault_ac，觀察型）**：初報（一組壓縮機未做動、風扇有做動、溫度設定與自動模式正常）→ ①OCC 先叫**關閉空調十秒鐘後再開啟**（行控實務第一步，純報話）→ ②CMS「空調重置」（鎖鈕）→ ③CMS「TCU錯誤復位」（鎖鈕）→ 恢復「壓縮機恢復做動，空調恢復正常」→ 繼續行駛持續留意觀察。

**二組以上（fault_ac_2plus，專屬 builder buildAc2Chunks）**：一組三步全走（仍未做動）→ ④OCC「確認**駕駛端**應急按鈕「空調關閉」是否被按壓」（單端即可，不確認兩端）→ 回報未被按壓（**只做未按壓主線**）→
- **下行**：續行 V02 2月台 → OCC「抵達V01後執行清車不載客重開機，**並執行A步驟**」→ 司機員完整敘述A步驟回報（「已完成清車不載客重開機，於降弓關機後按壓應急按鈕「空調關閉」，開機升弓確認380V及750V投入後復歸按鈕，等候四分鐘確認壓縮機及風扇恢復正常做動，空調恢復正常」）→ 發車 → 通用收車尾段。
- **上行**：**直接收車**（V11 2-0／V26 換備用車），A步驟交回廠處理。

**實測**：一組 4路線×10、二組 4路線×10（下行7步/上行5步、A步驟句逐字、收車 faultKey）＋512 runs 回歸，累計 5349 項全過、0 console error。

**補充（2026-07-11 空調視覺＋報實際組別）**：資料夾找到原廠 HMI 空調頁截圖（`資料\lrt-manual-images\圖_4.21-1/-2_HMI_HVAC_*.png`）。依圖 4.21-2 在模擬 CMS 空調頁新增 M2/M4 機組視覺：每組風扇（內嵌 SVG 三葉扇）×1＋壓縮機雪花（Unicode ❄）×2，共 4 顆（M2.1/M2.2/M4.1/M4.2）；圓形灰底、藍=運作中、白=未做動（同原廠語彙）。一組隨機熄 1 顆、二組以上隨機熄 2~4 顆，風扇全程維持藍；恢復/A步驟完成後回藍。台詞升級報實際組別：一組走 FAULT_DATA `{AC}` 記號（renderer 置換 persistentFault.acUnits）、二組由 buildAc2Chunks 直接帶入。診斷欄同步警示。實測含視覺熄燈矩陣/恢復歸位/組別台詞一致性，5488 項全過。

---

## 校對 #6：ATP 大項（2026-07-11 定稿＋實測；四子項留二刪二）

**留**：
- **超速（E3516）**：初報「輔助速限裝置顯示E3516，已將主控制器拉至停機檔位並放開DSD，確認ATP重置鍵紅燈亮起，列車無法移動」→ OCC「請按壓ATP重置鈕後回報」（鎖 ATP重置鈕）→ 回報「ATP面板未回到初始模式，列車仍無法行駛」→ OCC「請執行Key Off Key On」（純報話）→ 仍無法移動 → Bypass{MC}（鎖鈕）→ 無效 → 播廣播＋復歸＋降弓重開機 → 通用收車。
- **AS/AE（E3904，使用者確認代碼）**：初報「CMS出現AS煞車圖示，輔助速限裝置顯示E3904，…」餘四步完全同超速；只做「有代碼」主線。

**刪（使用者指示）**：讀取錯誤（故排同超速，無另做必要）、平面段故障（不做）。皆從下拉／隨機池／FAULT_DATA／CMS 視覺移除。

**重置鈕視覺（使用者要求找圖後自製）**：手冊 226 張圖無 ATP 面板特寫（S131-135 為架構圖；PDF 無法轉圖）；依文字規格（重置鍵附紅燈、INIT=20、OFF、E碼）自製實體圓鈕「重置」＋紅色 LED（`atp-reset-led`，CSS `.atp-reset-btn/knob/led`）；atpFault() 亮燈、恢復即滅；點擊行為沿用 handleCMSClick('ATP重置')。

**實測**：超速＋AS/AE 各 4 路線×12 逐句（高架/平面 MC 皆中）＋重置鈕紅燈亮/滅視覺＋480 runs 回歸，6839 項全過、0 console error。

---

## 校對 #7：車門 一組以上故障／無法授權（2026-07-11 定稿＋實測）

**車門編號規則（使用者定，入報話規則 §3）**：M1L/M1R…（車廂＋左右側）；**M3 無車門**；V01 發車＝M1 端在前、V11/V26 發車＝M5 端在前。

**一組以上車門故障（buildDoorChunks）**：強制站內；隨機故障門（8 選 1）。①CMS「車門全開全關」（鎖兩段鈕）→ ②無效→OCC 授權離開駕駛室至故障車門確認狀態並隔離 → ③現場隨機開/關分支：**開→「已拉下EED並完成隔離，確認車門按鈕亮紅燈及地板指示燈亮紅燈」**／**關→「已直接完成隔離」**→ OCC「執行清車作業，完成後按壓應急按鈕「上下車門」{MC}，續行收車」（鎖鈕）→ 回報後接通用收車。**無重開機**；收車段 CMS 門維持隔離顯示。CMS 視覺＝故障門 door-box 離線＋診斷警示。

**無法授權（buildDoorNoauthChunks）**：強制站內；初報「月台側車門無法授權」。①CMS 車門全開全關 → ②收回授權→按「零速度」再釋放開門訊號（鎖紅鈕）→ ③隨機兩結局：**成功**＝可授權開門→OCC「停站秒數結束收回車門授權後復歸零速度，繼續行駛持續留意觀察」（recovered）；**失敗**＝OCC「授權操作第一道車門供旅客上下車，並廣播告知旅客相關資訊，繼續行駛持續留意觀察」（門況維持）。觀察型不收車。

**實測**：一組 4 路線×12（開/關分支、站內強制、MC、視覺離線/歸位）＋無法授權 4 路線×12（兩結局）＋480 runs 回歸，累計 8066 項全過、0 console error。

---

## 📦 CONTEXT housekeeping 搬移（2026-07-11）：煞車系統開發全紀錄＋校對#1/#2摘要＋舊筆記

## 🔧 進行中：煞車系統故障（2026-06-27 開工，待下次續）

**使用者已確認的方向：**
- 煞車要**拆成多個下拉選項**（比照牽引）。
- 步驟1 司機員報**實際故障轉向架**：例「目前M1轉向架ERR，CMS顯示E1，OVER」（M1/M3/M5 為隨機那一個）。
- 步驟3 **司機員不主動詢問**，由 OCC 直接下達重開機指示；**不用通用結尾**。
- 要新增**轉向架 SE**（§2.7）的處置流程。

**下拉拆三個**：煞車-轉向架ERR(E1)／煞車-轉向架SE／煞車-兩組以上(降弓重開機,§2.1後半)。

**✅ §2.1 轉向架 ERR／單一轉向架 SE — 綠線下行台詞（2026-07-07 逐句定稿）：**

| 步驟 | 角色 | 台詞 |
|------|------|------|
| 1 | 司機員 | `[站間,如V04往V03]，101車呼叫OCC，目前M1轉向架ERR，CMS顯示E1，列車無法移動，OVER`（單一SE版：`…目前M1轉向架SE，CMS顯示SE…`）|
| 1 | OCC | `OCC收到，101車，請執行CMS「隔離轉向架」作業，確認列車是否恢復，OVER` |
| 1 複誦 | 司機員 | `101車收到，執行CMS「隔離轉向架」作業，通話完畢。` |
| 2 | 司機員 | `101車回報，已完成隔離M1轉向架並按壓煞車解除，列車可移動，OVER`（煞車解除由司機員於隔離時自按，OCC不另指示）|
| 2 | OCC | `OCC收到，請101車繼續行駛，抵達V02 2月台回報，OVER`（**固定 V02 2月台**，因下一步一定在 V01 清車重開機）|
| 2 複誦 | 司機員 | `101車收到，繼續行駛抵達V02 2月台回報，通話完畢。` |
| 3 | 司機員 | `101車回報，已抵達V02 2月台，OVER` |
| 3 | OCC | `OCC收到，請101車抵達V01後執行清車不載客重開機作業，OVER` |
| 3 複誦 | 司機員 | `101車收到，抵達V01後執行清車不載客重開機作業，通話完畢。` |
| 4 | 司機員 | `101車回報，已於V01完成清車不載客重開機，列車恢復正常，OVER` |
| 4 | OCC | `OCC收到，請101車確認前方號誌開通後依時刻表發車，抵達V10 1月台手動清除路徑回報，OVER` |
| 4 複誦 | 司機員 | `101車收到，確認前方號誌開通依時刻表發車，抵達V10 1月台手動清除路徑回報，通話完畢。` |
| 5 | 司機員 | `101車回報，已抵達V10 1月台並完成手動清除路徑，OVER` |
| 5 | OCC | `OCC收到，請101車稍後變更目的地為V11 2月台，抵達後執行清車回報，OVER` |
| 5 複誦 | 司機員 | `101車收到，稍後變更目的地為V11 2月台，抵達後執行清車回報，通話完畢。` |
| 6 | 司機員 | `101車回報，已抵達V11 2月台並完成清車，OVER` |
| 6 | OCC（終）| `OCC收到，請101車確認前方號誌開通後，行駛至臨時駐車位置2-0交接後回報，OVER` |
| 6 複誦 | 司機員 | `101車收到，確認前方號誌開通行駛至臨時駐車位置2-0交接後回報，通話完畢。` |

> 收尾不加「已交接/OCC通話完畢」（交接屬值班台工作）。步驟2 燈號句已移除（不確認總故障燈）。

**🔑 收車流程重大通則（使用者 2026-07-07）：所有故障清車都走完整流程**。收車尾段：**綠線**＝V10 1月台手動清除路徑 → 變更目的地 V11 2月台執行清車 → 2-0交接。**藍線**（無清除路徑，目的地本就是 V26 1月台）＝抵 V26 1月台清車換端「故障車停此」 → 攜原車裝備至 **V26 2月台藍海備用車**開啟載客模式。
- **綠線下行**：可移動→V02 2月台回報→**V01 清車不載客重開機**→發車→收車尾段→2-0。
- **綠線上行**：**不重開機**，可移動→收車尾段（V10 1月台清除路徑→V11 2月台清車→2-0）。
- **藍線下行**：同綠線，端點在 **V01 清車重開機**，發車後**直接開回 V26 1月台清車換端（中間不回報）**，再攜裝備至 V26 2月台藍海備用車。
- **藍線上行**：**不重開機**，直接走藍線收車尾段。
- ✅ ERR／單一SE 四種變體（綠上/綠下/藍上/藍下）**台詞全定稿**。
- 藍線收尾＝**故障車停 V26 1月台**、司機員攜原車裝備轉乘 **V26 2月台藍海備用車**開啟載客模式（非開到 2-0）。
- **§3.4（380V，已實測）也要一起改走完整流程**（使用者確認）；改動需另小心處理＋重測。

**✅ SE 邏輯已確認（2026-07-07）：**
- SE 下拉要**再分兩種**（對應手冊 §2.7 step1「確認單一 or 全部轉向架 SE」）：
  - **單一轉向架 SE** → 處置**同 ERR**（CMS 隔離該轉向架＋煞車解除）。
  - **全部轉向架 SE（或檔位顯示 SE）** → **重置安全煞車 → 復歸 → 無效則重開機**（不隔離、不走 CMS 隔離）。
- 決定：模擬「行駛中站間發生」；高架段一樣切機廠模式；SE 下拉**隨機出單一/全部**讓學員練習分辨（已確認）。
- 單一 SE 台詞＝上面 ERR 那張表（共用）。
- ✅ **全部轉向架 SE／檔位顯示 SE 台詞定稿**（2026-07-07）：
  - 步驟1 司機員報「全部轉向架SE、檔位顯示SE、列車無法移動」。
  - OCC 指示「重置安全煞車」（**OCC 指示、非 CMS**；**重置＋復歸是同一步**＝按下再按回，故台詞只一句「重置安全煞車」）。
  - 無效 → OCC 指示「**播放預錄廣播告知旅客相關資訊後，執行降弓重開機**」（因列車無法移動→**就地重開機**，不到端點）。
  - 恢復可移動後（因已就地重開機，端點**不再重開機**）→ 收車。**關鍵：能收車的地方只有 V11（綠→2-0）／V26（藍→換藍備）；V01 只能換端**。
    - 所以收車台詞**不分上下行**：下行只是先到 V01 正常換端（正常營運、**無特別台詞**），再上行到 V11/V26 收車。
    - **綠線收車**：V10 1月台手動清除路徑 → 變更目的地 V11 2月台執行清車 → 2-0 交接。
    - **藍線收車**：V26 1月台清車換端 → 攜原車裝備至 V26 2月台藍海備用車、開啟載客模式。
- ✅ **煞車-兩組以上轉向架 ERR（降弓重開機）台詞定稿**（2026-07-07）：
  - 步驟1 司機員報「M1、M3轉向架ERR，CMS顯示E1，列車無法移動」（M1/M3/M5 **隨機 2 組**；此情況機率很低）。
  - OCC **直接**播廣播＋降弓重開機（不隔離、不重置）→ 恢復→ 收車段**完全同全部 SE**。
- 🔑 **通則：所有故排初報都含「列車無法移動」**（使用者 2026-07-07）；恢復後回報「列車恢復正常、可移動」。

**✅ 煞車三下拉台詞全定稿（2026-07-07）**：ERR(單一,隔離)／SE(隨機 單一=同ERR／全部=重置→降弓重開機)／兩組以上(直接降弓重開機)。下一步＝寫程式。

**✅ 通用位置回報規則（使用者 2026-07-07 指正並已實作＋測試）：**
- 站間報**相鄰兩站**：`V04往V03`（方向靠站號順序，**不加**「之間途中」與上下行）；不可報到終點（如「V04往V01」）。
- 單一路口：`J04上行/下行`；會車區：`V01會車區下行` 等（無方向性→**加上下行**）。
- 原則：格式已含方向就不加、單點無方向才加上下行。
- **已改的程式**：`getAdjacentFaultLocation()`（line 302 及 fallback 278/289）改輸出 `Vxx往Vyy`；PEI 情境（line 622）改用 `getAdjacentFaultLocation`（修好跨站 bug）；紅燈 downstream 正則（line 638）改 `/往(V\d+)/`；路口延誤情境（line 624-625）loc 加上下行。
- **已測**：Node 抽真實函式跑 4 條路線 × 40 次，格式正確、無「之間途中」殘留、downstream 正則 OK。⚠ 尚未跑瀏覽器 Playwright（留待 SE 台詞完成一起走查 render/console）。
- 規則已寫進 `報話通用規則.md` §3。

**🚧 實作進度（2026-07-07 開工）：**
- ✅ **STAGE 1**：`buildPickupChunks` 收車 helper 改新規則（藍線改「V26 1月台清車換端→V26 2月台藍海備用車」、移除 V27 清除路徑；綠線維持）。**同時修正了 §1.5 的藍線收車**。
- ✅ **STAGE 3**：§3.4 `buildV380Chunks` 結局B 改呼叫 `buildPickupChunks`（走完整流程）。
- ✅ **STAGE 2 完成**：拆 3 下拉（`fault_brake_err/se/2plus`）＋buildPoints 注入（子類型/轉向架/線別/方向＋設 persistentFault）＋新 `buildBrakeChunks`＋`updateCMSDisplay` 支援單一/全部/兩組＋makeChunks dispatch＋persistentFault 加 bogies/subtype 欄位。
- ✅ **jsdom 白箱實測全綠**：台詞矩陣(4子型×綠藍上下行)零錯、注入→對話→CMS標籤 24/0、§1.5/§3.4 回歸 14/0、0 console 錯誤。
- 🐛 **使用者線上實測抓到 2 bug（2026-07-07，已修＋驗證）**：
  - **Bug 1 CMS/台詞轉向架不符 + Bug 收車後冒出「起始/V01」**：同根因＝`buildPoints` 對**多段行程**（如 V11→V26 拆兩段）**每段都注入故障**（覆蓋 persistentFault）＋故障收車後**照接正常終點**。修法：加 `faultDone` 旗標→故障只注入一次、`break` 不處理後續 leg、`if(!faultDone)` 不接 finalPt。（此修正也讓 §1.5/§3.4 更乾淨）
  - **Bug 2 隔離後「煞車解除」鈕沒跳出／隔離完就算完成**：改成**複合動作**——單一 ERR/SE 用 `requireAction:'隔離並煞車解除'`＋新旗標 `brakeReleaseStep`：CMS 隔離正確轉向架後**跳出「煞車解除」應急鈕**，**按了才算故排完成**（改 buildBrakeChunks、handleCMSClick、handleEmergencyClick、render 共 6 處）。全部SE/兩組的降弓重開機、重置安全煞車**不需按鈕**（使用者確認，純報話）。
  - ✅ **互動模擬測試 5/0**：隔離→跳煞車解除鈕→只隔離不算完成→按鈕後才完成，全過。
- ✅ **全面邏輯掃描通過**：10 情境 × 5 路線 × 6 次＝300 run，零 badReq(卡死)/空台詞/undefined/雙故障/CMS不符/殘留終點，0 console 錯誤。單一ERR/SE 確認有「隔離轉向架→煞車解除」兩鈕。
- ⚠ 仍未自動化：真實滑鼠點 CMS 隔離＋按鈕的端到端 UI（jsdom 測到函式與 requireAction 合法性，但沒模擬點擊）＋視覺截圖。**建議使用者再線上點一次確認手感**。

**🛠️ 實作計畫（台詞已全定稿，照此寫程式）：**
1. **下拉**：line 237-239 把 `fault_brake` 拆成 3 個 option：`fault_brake_err`（單一ERR,隔離）／`fault_brake_se`（隨機單一SE=隔離／全部SE=重置→降弓重開機）／`fault_brake_2plus`（兩組ERR,降弓重開機）。randomPool（line 604）也對應調整。
2. **注入**：buildPoints 故障點（line ~617）決定並存於 pt：故障類型、轉向架（單一=M1/M3/M5挑1；兩組=挑2）、tag(ERR/SE)、line(綠/藍,由leg判)、direction(上/下行,由leg朝V01=下行)。`persistentFault.bogie/tag` 由此注入（取代 line 466 惰性亂數）。
3. **自訂 builder**：新增 `buildBrakeChunks(trainId, cur)`（比照 `buildRedlightChunks` line 636）：
   - 單一ERR/單一SE：報故障(含列車無法移動)→OCC隔離轉向架(requireAction, CMS)→回報可移動→收車。
   - 全部SE：報(全部轉向架SE,檔位SE,無法移動)→重置安全煞車→無效→播廣播+降弓重開機→恢復→收車。
   - 兩組ERR：報(2組ERR,E1,無法移動)→直接播廣播+降弓重開機→恢復→收車。
4. **共用收車 helper** `buildCollectionChunks(trainId, line, needEndpointRestart)`：
   - 下行且 needEndpointRestart(ERR/單一SE未重開機)：先 V02 2月台→V01清車不載客重開機→發車。全部SE/兩組(已就地重開機)：下行只到V01正常換端(無台詞)。上行：跳過。
   - 綠尾段：V10 1月台手動清除路徑→變更目的地V11 2月台執行清車→2-0交接。
   - 藍尾段：V26 1月台清車換端→攜裝備至V26 2月台藍海備用車開啟載客模式。
5. **makeChunks**：加分支 `cur._isBrakeScenario → buildBrakeChunks`（line ~790 旁）。
6. **CMS 互動**：單一ERR/單一SE 沿用既有隔離解鎖（line 463-534）；全部SE/兩組 無CMS操作（降弓重開機純報話, requireAction 用「降弓重開機」但無按鈕→比照紅燈 requireAction 機制）。
7. **§3.4** `buildV380Chunks`（line 711）結局B 改呼叫 `buildCollectionChunks` 走完整流程；改後**重測 §3.4**。
8. **Playwright**：裝 chromium，逐一走查 3 下拉 ×（綠/藍 上/下行）＋SE隨機兩型：0 console error、台詞正確、ERR CMS隔離可解鎖。

**✅ 校對 #1 完成：牽引-T字亮未紅燈(Bypass)（2026-07-08 定稿＋實測）**
- **定稿流程**：初報（T字亮、總故障燈未紅、主控無出力、無法移動）→ OCC 授權按 Bypass（**高架段 V01–V07 才帶「並切換機廠模式」**，平面不帶）→ 無效回報 → OCC「播放預錄廣播告知旅客相關資訊後，**復歸應急按鈕**並執行降弓重開機」→ 恢復回報「列車恢復正常、可移動」→ 通用收車（已就地重開機型：下行 V01 只換端無台詞）。逐句台詞見 `工作日誌封存.md`。
- **通用 renderer 擴充（後續情境校對可直接沿用）**：FAULT_DATA step 新增選用欄位——`type:'report'`（後續回報，開頭「{車號}車回報」不帶位置）、`occ`/`echo`（自訂 OCC 指示與複誦句）、`mcMode:true`＋report 內 `{MC}` 記號（依 `_elevated` 動態帶「並切換機廠模式」）。注入點以 `isElevatedLoc(faultLoc)` 判斷高架（V 編號全 ≤7 且無 J 路口）。無這些欄位的既有情境輸出完全不變（回歸已驗）。
- **實測**：jsdom 台詞矩陣 4 路線×40 次（高架 76／平面 84，兩分支皆中）＋ 31 情境×10 次回歸 310 runs，全部 0 失敗、0 console error。`fault_traction_tcu` 舊式結尾確認未被誤動。

**✅ 校對 #2 完成：牽引-CMS T字燈號消失(TCU離線) `fault_traction_tcu`（2026-07-10 定稿＋jsdom實測）**
- 三裁決點全拍板：①刪 Bypass；②可移動性採通則（初報「無法移動」、復位後「恢復可移動，惟牽引圖示仍離線」）；③隨機熄 1～2 顆 T 燈三情形都做（動力轉向架僅 **M1、M5**，各 2 顆 T 燈）。
- 程式：`FAULT_DATA` 移除 tcu、新增 `buildTcuChunks`＋注入分支＋`persistentFault.tLights`；`buildPickupChunks` 加 `fkOverride` 參數（上行未重開機收車段 CMS 維持故障）；CMS 視覺改熄實際 T 燈（移除舊版誤植檔位 E1）、復位後 T 燈維持熄滅至 V01 重開機。
- 實測：4 路線×30 次台詞矩陣＋CMS 互動＋32 情境×512 runs 回歸，1634 項檢查全過、0 console error。逐句台詞與細節見 `工作日誌封存.md`。
- ⚠ **建議使用者線上點一次確認手感**（jsdom 未模擬真實滑鼠點擊與視覺截圖）。

**▶️ 舊筆記（2026-07-07 更新）**：**A/B 類全部草稿＋高中優先 CMS 視覺已搭建完成**，進入「逐項人工校對」階段。
- **權威依據**：故排一律以「列車應變處理步驟」為準（`lrt-contingency-steps.md`/`.json`）。做/改任何情境先查這份。
- **現況**：三批共 ~20 個 A/B 情境全走同一套框架（`_isFaultScenario` renderer + `ending` 欄位：restart→通用收車／observe→繼續觀察），語氣沿用煞車系統。全部通過 jsdom 測試＋300 次掃描 0 例外。詳見下方「待處理事項」表與批次紀錄。
- **下一步＝使用者逐項校對**（一次一大項、確認措辭與細節後才定稿）。校對時可一併決定：各情境收車終點是否需個別調整、低優先視覺（空調二組）是否要補、借用/代理按鈕是否新增。
- **housekeeping 待辦**：煞車＋各批次逐句台詞細節可移到 `工作日誌封存.md` 精簡本檔（尚未做，不急）。
