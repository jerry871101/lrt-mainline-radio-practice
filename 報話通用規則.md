# 淡海輕軌正線無線電｜報話通用規則速查

> **用途**：每開始一個新故障情境前，AI 先讀本檔，把這些跨情境通用規則先套用，減少逐句來回。
> 這些規則是從使用者歷次定稿中歸納出來的「實務慣例」，非手冊原文。

---

## 1. 發話權與流程

| 規則 | 說明 |
|------|------|
| **OCC 主動下達** | 司機員**不主動請求/詢問**「是否重開機、是否發車、是否收車」等。一律由 OCC 主動下達指示，司機員只回報狀態與複誦。 |
| 三段式 | 司機員回報 → OCC 指示（鸚鵡式複誦狀態 + 下指令）→ 司機員複誦指令。 |
| 故障排除步驟 | 司機員回報故障 → OCC 指示執行某操作 → 司機員執行後回報結果 → （無效則）OCC 指示下一步。 |
| 初報含「無法移動」 | **所有故排**司機員初報一律帶「**列車無法移動**」；故排成功後回報「**列車恢復正常、可移動**」。 |
| 降弓重開機前先播廣播 | 需就地「降弓重開機」時，OCC 一律先指示「**播放預錄廣播告知旅客相關資訊**」後再執行。 |
| 兩種結尾（2026-07-07 定）| **① 收車型（restart）**：`已執行降弓重開機，列車恢復正常` → OCC「重開機後不載客，續行收車」→ 接收車尾段。**② 觀察型（observe）**：故障排除成功但不需收車（如 ATP 平面段、24V 一組、架空線斷電仍升弓）→ OCC「請繼續行駛，持續留意觀察，通話完畢」。 |

### 1.1 故排三段式標準模板（2026-07-07 定，所有故排通用）

```
每一步：
  司機員：{位置}，{車號}呼叫OCC，{故障描述}，列車無法移動，OVER
  OCC   ：OCC收到，{車號}，請執行CMS「XXX」作業  ／  請按壓應急按鈕「XXX」，確認列車是否恢復，OVER
  司機員：{車號}收到，執行CMS「XXX」作業 ／ 按壓應急按鈕「XXX」，通話完畢。
恢復回報：
  司機員：{車號}回報，已…，列車恢復正常（可移動），OVER
  OCC   ：（收車型）OCC收到，{車號}重開機後不載客，續行收車，OVER
          （觀察型）OCC收到，請{車號}繼續行駛，持續留意觀察，通話完畢。
```

> OCC 的動詞用「**請執行CMS「」作業**」或「**請按壓應急按鈕「」**」，取決於該操作屬 CMS 或應急按鈕（見 §1.2）。這是煞車系統定稿的語氣，全故排一致沿用。

### 1.2 CMS 操作 vs 應急按鈕（決定 OCC 措辭）

| 類別 | OCC 措辭 | 目前按鈕 |
|------|---------|---------|
| **CMS 操作** | 請執行CMS「X」作業 | TCU錯誤復位、隔離轉向架、車門全開全關、空調重置、後視鏡重置、**ATP重置** |
| **應急按鈕** | 請按壓應急按鈕「X」 | 列車自動保護 Bypass、煞車解除、集電弓降弓 HV OFF、上下車門、降級模式、**零速度** |

> ⚠ **ATP重置 ≠ 列車自動保護（2026-07-07 使用者澄清）**：ATP重置是 **CMS/ATP 面板**操作（放在 CMS 首頁 ATP HMI 下方）；ATP 對應的**應急按鈕是「列車自動保護」（Bypass）**。兩者對應不同功能，勿混用。
> **零速度**是應急按鈕（紅色），用於「車門無法授權」釋放開門訊號。

### 1.3 高架段機廠模式（寫死規則，2026-07-10 使用者訂）

**「降級模式」「列車自動保護 Bypass」「上下車門」三顆應急按鈕，在高架段（V01–V07）按壓時一律加「並切換機廠模式」，適用於每一個情境。**
- 程式：通用 renderer 以 `MC_BUTTONS` 清單自動判斷（`_elevated` 才帶入），FAULT_DATA 不必逐情境設 `mcMode`；自訂 occ/echo 句可用 `{MC}` 記號。
- 平面段不帶；復歸時機依各情境定稿。

## 2. 結尾語

| 結尾 | 意義 |
|------|------|
| `OVER` | 我講完換你講（每段呼叫/回應中間） |
| `通話完畢` | 整組對話結束、釋放頻道（該次任務最後一句） |

## 3. 位置與元件描述

| 規則 | 範例 |
|------|------|
| 報**站間**（相鄰兩站）| `V04往V03`（方向靠站號順序表達）；**不加**「之間途中」、**不加**上下行；不可跨站報終點（不可說「V04往V01」） |
| 報**單一路口** | `J04上行` / `J04下行`（無方向性，**須加上下行**） |
| 報**會車區** | `V01會車區下行` / `V11會車區上行`（無方向性，**須加上下行**） |
| 報**站內** | `V09 1月台` |
| 方向原則 | **格式本身已含方向（如 `V04往V03`）就不加上下行；單點無方向性（路口/會車區）才加上下行** |
| 報**實際元件** | 報實際故障件：`M1轉向架ERR`、`CMS顯示E1`、`380V亮黃燈`、`M1 380反灰`；不講通稱 |
| 上下行月台 | 上行 V01→V11/V26 停 **1月台**；下行 V11/V26→V01 停 **2月台** |

## 4. 重開機 / 清車

| 規則 | 說明 |
|------|------|
| 重開機鈕**不在 CMS** | 本練習**不放重開機按鈕**，重開機步驟為純報話。 |
| 不重複清車 | OCC 下「清車重開機」=清車屬程序內；下「重開機」也不在開機後再清車。**勿出現兩次清車**。 |
| 「列車恢復正常」 | 現場術語，重開機/故排成功後司機員用語，直接沿用。 |

## 5. 故障恢復後的視覺

- 故障排除成功 → CMS 視覺（gear、圖示）回正常、**總故障燈回白**（程式上把該段 chunk `faultKey:'recovered'`）。

## 6. 收車流程模板（重開機/故排恢復後）

> **通則（2026-07-07 確認，適用所有故排）：**
> - **能收車的地方只有 V11（綠→2-0 交接）／V26（藍→換藍海備用車）；V01 永遠只能換端、不能收車。**
> - 所有故障清車一律走「完整流程」（不再分情況 A/B；§3.4 亦改為完整流程）。
> - **下行**故障最後都要先到 V01（換端），再上行到 V11/V26 收車；**上行**則直接到 V11/V26 收車。**收車台詞不分上下行。**

**端點要不要「清車重開機」＝看故障有沒有在線上重開機過：**
- **未重開機的故障**（如 ERR／單一 SE：隔離後可動但尚未重開機）：下行到 **V01 執行清車不載客重開機**，恢復後接「收車尾段」。
- **已就地重開機的故障**（如全部 SE：無法移動→站間降弓重開機）：到 V01 只**正常換端（無特別台詞）**，接「收車尾段」。

**收車尾段（綠、藍不同）：**
- **綠線**：① 抵 **V10 1月台手動清除路徑**回報 → ② **變更目的地 V11 2月台執行清車**回報 → ③ 行駛至**臨時駐車位置 2-0 交接**。
- **藍線**（**無清除路徑步驟**，因目的地本就是 V26 1月台）：① 抵 **V26 1月台執行清車換端**回報 → ② 故障車停 V26 1月台，攜原車裝備至 **V26 2月台藍海備用車**，開啟載客模式。

> 收尾**不加**「已交接／OCC通話完畢」（交接屬值班台工作）。

## 7. 總故障燈判讀（手冊 §16）

| 燈色 | 含意 |
|------|------|
| 白 | 正常 |
| 黃 | 撒砂異常；電韌異常 1/4 |
| 紅 | EB1/2 作動、牽引安全迴路作動（車門開啟/授權、拖救、停機模式、安全煞車…）、電韌異常 1/2 |

380V 閃電圖示：白＝正常、黃＝動力問題（**非紅**）。

## 8. 高架 / 平面 與機廠模式

- **高架段 V01–V07**（無路口）；J01（V07 側）起轉平面。
- 在**高架段**按降級模式/Bypass 等，需**切換機廠模式**。

## 9. 常用術語對照

| 術語 | 意思 |
|------|------|
| 反灰 | CMS 監視畫面上某項目變灰（離線/失效），如「M1 380 反灰」 |
| 重交接 | 回到駐車位置重新做勤務交接 |
| 清車換端 | 清空旅客後換駕駛端（終點站常見） |
| 復歸 | 把先前按下的應急按鈕/狀態解除回原狀 |
| 降級模式 | 應急按鈕，繞過部分保護讓列車能低速移動 |
| 機廠模式 | 行車模式之一，高架段故障處置常需切入 |
| 端點站 | 路線端點（V01／V11／V26），換端、清車、收車多在此 |
| 下游站 | 行車方向的下一個車站 |

---

## 既有故障情境的「定稿模式」參考

- **牽引 §1.5**：依站內(月台)/站間分支；站內→車門全開全關確認牽引力（先全開後全關）；站間→降級模式至下游站；皆 OCC 下達清車重開機→恢復→收車（情況A）。
- **牽引 §3.4**：隨機兩結局；A=TCU 復位有效→「請繼續行駛，持續留意觀察」；B=降級(高架切機廠)→下游站復歸→復歸無效→再降級+端點站清車重開機→收車（情況B）。
- 詳細逐句台詞見 `index.html` 的 `buildRedlightChunks` / `buildV380Chunks` / `buildPickupChunks`。
